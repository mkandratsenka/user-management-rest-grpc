server:
  port: 8090

spring:
  application:
    name: api-service
  threads:
    virtual:
      enabled: true

grpc:
  client:
    user-service:
      address: static://localhost:9090
      negotiation-type: plaintext